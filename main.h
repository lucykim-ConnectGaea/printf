#ifndef HEADER_FILE
#define HEADER_FILE

#include <stdarg.h>
#include <stddef.h>
#include <stdlib.h>

int _putchar(char c); /*writes the character c to stdout */
int _printf(const char *format, ...);
#endif
